{
  "name": "roxiler-project-main",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start", 
    "client": "cd frontend && PORT=3001 npm start" 
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
